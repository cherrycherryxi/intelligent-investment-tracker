Use a Task sub-agent (subagent_type: Explore) to map this codebase area and return:
1. Module/file map with each file's responsibility (one line each)
2. Key entry points, public APIs, or exported symbols
3. Internal dependency graph — which files import which
4. Data flow: how data enters, transforms, and exits this area
5. Any dead code, duplication, or surprising patterns

Focus area: $ARGUMENTS

Search breadth: very thorough. Read-only — no file modifications.
Report back in under 400 words, structured with headers.
